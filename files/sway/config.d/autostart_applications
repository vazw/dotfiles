# Desktop notifications
exec ~/.config/sway/scripts/notifications &
# exec /usr/share/asus-numberpad-driver/.env/bin/python3 /usr/share/asus-numberpad-driver/numberpad.py up5401ea &
exec asus-numpad &

# Pipewire
exec rm /run/user/1000/pipewire-0.lock &
exec pipewire
exec easyeffects -wl 'External Speaker'
exec pactl set-default-sink alsa_output.pci-0000_03_00.6.analog-stereo
exec wpctl set-mute @DEFAULT_AUDIO_SOURCE@ 1
# exec dex -a -s /etc/xdg/autostart/:~/.config/autostart/
# nm-applet
exec nm-applet --indicator
exec blueman-applet
exec nostr-relay-tray

exec thunar --daemon

# Sway Fader
# exec python3 ~/.config/sway/scripts/swayfader.py     
 

# Autotiling
exec autotiling

# Wlsunset
# exec ~/.config/sway/scripts/light.sh


# Welcome
# exec dex -a -s /etc/xdg/autostart/:~/.config/autostart/

# Clipman
# exec clipman restore
exec wl-paste -t text --watch clipman store

# Gnome Keyring
exec /usr/libexec/polkit-gnome-authentication-agent-1
exec gnome-keyring-daemon --daemonize --start --components=gpg,pkcs11,secrets,ssh

# for Touchpad gestures
# exec libinput-gestures-setup start

# Outputs
# exec kanshi

# Xdg DEsktop Portal
exec /usr/libexec/xdg-desktop-portal
exec /usr/libexec/xdg-desktop-portal-wlr

# Tracker daemon for gnome apps
# exec lxappearance-daemon

exec pkexec brillo -c 1
exec ~/.config/sway/scripts/restorebrightness.sh

# swayrd
exec_always swayrd
